import { IsoCurrencyPipe } from './iso-currency.pipe';

describe('IsoCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new IsoCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
